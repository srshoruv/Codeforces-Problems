#include <bits/stdc++.h>
using namespace std;

int main()
{
    int testCase;
    cin >> testCase;

    while (testCase--)
    {
        vector<int> vec(4);
        int first, second;

        for (int i=0; i<4; i++)
        {
           cin >> vec[i];
        }

        for (int i=0; i<4; i++)
        {
           first = vec[i];
           second = vec[i+1];
        }
        

    }

    return 0;
}